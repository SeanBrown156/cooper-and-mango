# Godot 4
.godot/
*.import
export_presets.cfg
*.translation

# macOS
.DS_Store

# Editor
.vscode/
*.swp

# Raw source material (photos, Ableton projects, PixelLab exports, sketches).
# This is intentionally excluded for now — decide whether to move it to
# Git LFS before the first big batch of assets lands (see docs/PRODUCTION_ROADMAP.md).
assets/source/*
!assets/source/.gitkeep
