# Godot 4
.godot/
*.import
export_presets.cfg
*.translation

# macOS
.DS_Store

# Editor
.vscode/
*.swp

# Raw source material (photos, Ableton projects, PixelLab exports, sketches).
# Folder structure (the reference photo drop zone, see docs/ART_BIBLE.md) is
# tracked via .gitkeep; actual media files are excluded until the Git LFS
# decision is made (see docs/PRODUCTION_ROADMAP.md).
assets/source/**
!assets/source/**/
!assets/source/**/.gitkeep
