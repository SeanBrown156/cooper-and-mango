# PixelLab tools

PixelLab MCP performs generation. These local tools validate the package-local
environment contracts and generated specifications before Godot composition.

```sh
python3 tools/pixellab/validate_tutorial_room_asset_spec.py
```
