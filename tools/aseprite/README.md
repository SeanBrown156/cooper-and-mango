# Aseprite tools

These tools perform palette work on provisional assets only. The recolour
wrapper refuses input or output paths outside a package `wip/` directory.

```sh
tools/aseprite/aseprite_recolour.sh input-wip.png output-wip.png
```
