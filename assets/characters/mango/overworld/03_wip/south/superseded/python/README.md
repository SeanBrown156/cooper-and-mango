# Python leg-motion experiment

This variant uses deterministic integer-pixel edits to move the visible legs
through alternating contact and passing positions. It is intentionally separate
from the PixelLab output for comparison.

Outputs: 8 native **16×20** PNG frames, a looping native GIF, and an enlarged
MP4 review video.
